$kind: http-request
description: Student registration validation. `Software Development` does not exist in `study_fields`, so the registration must fail with 422.
url: "{{base_url}}/api/v1/auth/register"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
        "full_name": "Validation Student",
        "email": "val.badfield@test.local",
        "password": "Student@12345",
        "password_confirmation": "Student@12345",
        "accept_terms": true,
        "role": "student",
        "field": "Software Development",
        "specialization": "Mechanical Engineering"
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test("Invalid field (not in study_fields) is rejected", function () {
          pm.expect(pm.response.code).to.eql(422);
          const json = pm.response.json();
          pm.expect(json.success).to.eql(false);
      });
    language: text/javascript
order: 3000
