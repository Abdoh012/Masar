$kind: http-request
description: Student removes a training from their saved list. Requires a student token.
url: "{{base_url}}/api/v1/trainings/unsave/{{training_id}}"
method: DELETE
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 8000
