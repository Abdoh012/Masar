$kind: collection
description: 'REGENERATION: Saved Training - Multi User Regression (Student A / Student B). Validates that is_saved in Search (/search/trainings?q=) and Filter (/search/trainings/filters) is computed live from the saved_trainings relationship (student_id + training_id), that each student''s state is independent, and that the backend identity comes from the bearer token (client-provided student_id is never trusted). IMPORTANT (data-safe): request 2 captures Student A''s original state and Student B''s original state BEFORE any mutation, and the final Restore request re-establishes the original relationship(s). No TRUNCATE, no delete of unrelated records. Uses separate student_a_token / student_b_token variables so the two logins never overwrite each other. is_saved is NEVER hard-coded as request data. PROCESSING NOTE: this folder was authored and validated against the backend contract, but Postman/CI execution is required to confirm it; the backend E2E already passes.'
order: 10000
