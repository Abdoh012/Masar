$kind: http-request
description: "Negative. `specialization` 92 (Mechanical Engineering) belongs to field 9, not field 12 (Computer Science). Verifies the field-specialization relationship is enforced. Expected: 422 with message `User field or specialization is incorrect.`"
url: "{{base_url}}/api/v1/auth/register"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
        "full_name": "Test Student",
        "email": "{{$randomEmail}}",
        "password": "{{register_student_password}}",
        "password_confirmation": "{{register_student_password}}",
        "accept_terms": true,
        "role": "student",
        "field": 12,
        "specialization": 92
    }
scripts:
  - type: afterResponse
    code: |-
      pm.test("Cross-field specialization is rejected", function () {
          pm.expect(pm.response.code).to.eql(422);
          const json = pm.response.json();
          pm.expect(json.message).to.eql('User field or specialization is incorrect.');
      });
    language: text/javascript
order: 3000
