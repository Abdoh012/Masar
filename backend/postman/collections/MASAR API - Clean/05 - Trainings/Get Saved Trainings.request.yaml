$kind: http-request
description: List of the authenticated student's saved trainings (items + pagination). Requires a student token.
url: "{{base_url}}/api/v1/trainings/saved/list"
method: GET
headers:
  Accept: application/json
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 7000
